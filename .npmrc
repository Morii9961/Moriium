# pnpm 11 no longer honours `save-exact` here and only warns for
# `engine-strict`. Both now live in pnpm-workspace.yaml as `saveExact` and
# `engineStrict`. Add registry or auth settings here; keep pnpm behaviour
# settings in pnpm-workspace.yaml so they actually take effect.
