node_modules/
dist/
.astro/
.cache/
.pnpm-store/
.DS_Store
Thumbs.db

# Local-only plaintext and deployment credentials.
.private/
.env
.env.*
!.env.example

# Generated screenshots and local reports.
artifacts/
playwright-report/
test-results/
.astro-preview.*.log

# Local agent state. `.claude/worktrees/` accumulates whole checkouts with their
# own build output — tens of thousands of files — so a stray `git add -A` would
# otherwise commit hundreds of megabytes that are impossible to remove from
# history afterwards. Nothing in here is shared: `launch.json` names a Windows
# `pnpm.cmd`, so it would not work for anyone on another platform anyway.
.claude/
# Written into the workspace by the release state machine, never by hand: the
# export's articles, the media projection and the media manifest (ADR 0002
# sections 21.9 and 21.10). Anchored to the repository root, so an author's own
# src/content/posts/ tree and any nested public/ stay tracked.
/src/content/posts/exported/
/public/media/
/src/generated/
