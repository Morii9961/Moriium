{
  "name": "enouia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build",
    "preview": "node scripts/preview.mjs",
    "check": "astro check",
    "verify": "astro check && astro build && node --test tests/*.test.mjs && node scripts/verify.mjs"
  },
  "dependencies": { "astro": "7.2.4" },
  "devDependencies": { "@astrojs/check": "0.9.10", "typescript": "6.0.3" }
}
