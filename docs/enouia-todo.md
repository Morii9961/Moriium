# Enouia 的 Moriium TODO

最后整理：2026 年 8 月 30 日

这份清单只给后续开发使用。一次只拿一个待办；完成、验证、预览确认后，再取下一个。

2026-08-30，Morii 实际使用原型 B 后将其选为 vNext 最终路线，原型 A 取消。后续不再比较 A/B，而是补齐 B 并为 Hybrid 生产架构立 ADR。旧的公开站验收项仍是不能退步的基线；在 Phase 5 ADR 批准并更新项目合同前，生产站继续遵守现有静态架构与 `AGENTS.md`。

## 标记

- `[x]` 已实现并通过当前项目验证。
- `[~]` 已有代码基础，还没完成正式界面迁移或人工验收。
- `[ ]` 尚未开始。
- `[?]` 需要 Morii 决定。
- `[-]` 经 Morii 明确决定取消，不再开发。

## 当前基线

- [x] Astro 静态输出、TypeScript、pnpm 和 Nginx 部署边界已经确定。
- [x] 中文、日文、英文路由已建立：`/zh/`、`/ja/`、`/en/`。
- [x] A 方案已用于全局框架、正式首页和 Writing 页面。
- [x] 正式首页使用真实文章、分类、标签和更新时间，不再显示原型假数据。
- [x] 明暗主题可以切换并记住选择。
- [x] 首页和各基础页面的界面文案已经完成第一轮去 AI 味处理。
- [x] RSS、Sitemap、404、内容校验、链接检查和公开目录隐私审计已有实现。

## vNext 路线

### 00　当前站基线与重构冻结样本

- [x] 运行新的 `pnpm verify`：60 个 Astro 文件无诊断、30/30 测试通过、46 个静态页面构建成功，内容、媒体和链接检查通过；仍有既有的大型分包警告。
- [ ] 记录构建耗时、静态页面数、初始 JS、搜索索引和大型分包，作为 vNext 不能退步的基线。
- [x] 建立内容 fixture corpus：加密内容只用虚构正文、测试口令和测试密文；图片只用生成素材或已批准且已净化的公开衍生图；绝不读取真实 `.private/posts/` 或原始照片。语料在 `prototypes/fixtures/`，由 `pnpm -C prototypes fixtures:check` 断言校验，校验器本身已做负向测试。见 ADR 13.2。
- [ ] 将原 07–12 的人工验收要求转成可重复测试，优先覆盖灯箱焦点、按需加载、未知内容回退和三语关系。
- [x] Morii 确认全栈、登录、数据库与较大开发/运维工作量可以进入候选，体验优先。
- [ ] 将 T1–T10 改写成 B 的验收清单，覆盖登录、写作、自动保存、预览、发布、回滚、公开阅读、移动端与故障恢复；不再制作 A/B 评分表。
- [ ] 记录读者账户可能提供的具体价值与数据代价，不阻塞只含作者账户的 Phase 1；最终取舍留到 Phase 5。
- [?] Morii 决定哪些公开路由必须近实时或带登录态。
- [x] Morii 决定优先 Markdown 全量保真，还是优先 Tiptap 所见即所得。**2026-08-29 定夺：保真优先，但源码块必须可编辑，图片额外做成带预览的真节点。**保真仍是 11/11 逐字节一致。见 ADR 13.10、13.11、13.12。
- [ ] 为公网 Admin、数据库、备份和常驻服务列出可验收的安全与恢复要求。

### 01　原型 B 与路线选择

- [x] Morii 明确批准 Phase 1 的隔离代码范围、新依赖和服务边界后才开始。2026-08-29 由 Morii 本人批准 `adr-0001-phase1-spike.md`。
- [x] 建立 `prototypes/` 嵌套 workspace 骨架并验证隔离：四条验收标准全中，根配置零改动，`pnpm verify` 仍通过。见 ADR 13.1。
- [-] 原型 A：2026-08-30 经 Morii 决定取消，不再开发。A 没有经过失败验证，也没有输掉量化比较；Morii 根据 B 的实际体验直接结束了候选比较。
- [~] 原型 B：单一作者登录 + Tiptap + 自动保存 + 数据库版本 + 权限预览 + 测试发布。登录、Tiptap、自动保存、版本与回滚、发布、匿名读者视图已可操作（ADR 13.13），完整发布闸门（13.15）与图片属性面板（13.16）已完成；**只剩草稿的生产同源预览**。
- [x] 原型 B 使用隔离的虚构 fixture 与共享内容契约，不读生产数据，也不写回 `src/content/`。
- [-] A/B 任务耗时、点击数与维护成本对照：Morii 已根据 B 的实际体验结束候选比较。
- [x] 原型 B 的保存与发布已经分离、可回滚，且发布要过内容、翻译关系与媒体三条闸门；拒绝在事务里发生，不留公开指针也不留审计行。见 ADR 13.15。
- [x] Morii 于 2026-08-30 选择 B Hybrid 全栈为 vNext 主线；C 全站 SSR 仍须等待 06A/06B 的运行证据与第二份扩围 ADR。

### 02　共享内容、身份与媒体契约

- [~] 抽出 CLI、Studio 和 Astro 共用的 frontmatter/schema 校验，避免三套规则。原型侧已在 `prototypes/shared/content-schema.ts` 落地，并有与 `src/content.config.ts` 的字段漂移检查；生产侧仍是独立一份，合并要等 Phase 5。
- [~] 为每个自定义内容块定义稳定语法、属性、no-JavaScript fallback、feature marker 和版本策略。语法与 feature marker 已在 `prototypes/shared/content-blocks.ts` 成为可断言数据（11 个块，语料全覆盖）；**no-JavaScript fallback 与版本策略尚未定义**。
- [ ] 让 Studio 预览直接调用生产 remark/rehype 管线，不维护第二套近似解析器。
- [~] 定义摄影 asset manifest：公开路径、宽高、格式、alt、caption、版权和可公开 EXIF 白名单。形状与发布闸门已在 `prototypes/shared/media.ts` 落地（manifest 结构上没有原图路径字段），发布路由已经真的按它拦截（ADR 13.15），夹具清单在 `prototypes/fixtures/media/manifest.json`；实际的 manifest 生成仍未接线，图片引用也仍靠正则扫原文，会误挡围栏里的示例图片。
- [ ] Studio 媒体导入必须调用去 EXIF/GPS 与媒体检查；不得触碰原图。
- [ ] 加密文章继续留在忽略的 `.private/posts/`，不进入公开 Studio 数据接口。
- [~] 定义作者/管理员/可选读者权限、会话、CSRF、速率限制、审计和账户恢复。单一作者会话、CSRF、登录限速、草稿读取授权与发布审计已在 B 中落地；读者权限、账户恢复和生产级会话仍未定。
- [~] 定义数据库迁移、事务、备份恢复、canonical content 与派生 HTML 的信任边界。B 已固定 Markdown 为真源、版本追加与发布事务；迁移、备份恢复和派生 HTML 边界仍未完成。

### 03　选中写作系统的可用版

- [ ] 实现三语文章列表、翻译关系、草稿、新建、编辑和预览。
- [ ] 实现媒体导入、验证结果、数据库版本差异和失败恢复。
- [ ] 自动保存只写草稿；发布前必须通过内容与媒体校验。
- [x] B 的自动保存不等于发布，发布必须显式且可回滚，且必须先过完整发布校验。见 ADR 13.15。
- [ ] 增加单元、集成和端到端测试，覆盖作者认证、授权、事务、发布回滚和数据库故障。
- [ ] 用 B 的隔离测试库完成一篇文章的创建、修改、撤回草稿、显式发布和回滚。

### 04　公开站内容系统稳定化

- [ ] 完成下面保留的 07–12 验收项。
- [ ] 将容易漂移的正文正则 feature detection 逐步替换为解析结果生成的 manifest。
- [ ] 固定正文、普通媒体、摄影画廊和接近全宽媒体的宽度层级。
- [ ] 确认选中写作系统生成的内容与基准 fixture 在生产渲染上没有分叉。

### 05　B Hybrid 生产架构 ADR

- [x] Morii 已根据实际操作选择 B Hybrid 全栈，原型 A 取消。见 ADR 0001 第 13.14 节。
- [ ] 固定 B 的初始逐路由策略，明确哪些公开路由预渲染或缓存，哪些路由按需渲染。
- [ ] 明确作者与读者账户、canonical content、逐路由渲染策略和发布时效。
- [ ] ADR 定义 API、数据库、renderer、媒体、认证、备份、监控、安全和回退。
- [ ] 同步修改 `AGENTS.md` 与部署合同后才能进入生产实现。

### 06A　全栈 Hybrid 影子系统

> 只有 Morii 批准 05 的全栈 ADR 后才能开始。

- [ ] 只迁移 fixture 和测试文章，不先迁移正式内容。
- [ ] 实现作者认证、版本化 Public/Admin DTO、服务端可信 renderer、数据库迁移和备份恢复。
- [ ] Admin 与权限草稿按需渲染；公开文章优先预渲染或缓存。
- [ ] 测试 API 断开、SQLite 锁、备份恢复、草稿越权、媒体故障和静态回退。
- [ ] 双轨运行并保留当前静态站的可恢复版本，Morii 验收后再决定是否迁移正式内容。

### 06B　可选扩大公开 SSR

> 只有 06A 证明更多公开 SSR 可能有可感知收益，并且 Morii 批准单独的实验 ADR 后才能开始。

- [ ] 用 fixture 验证首页、文章、归档、分类/标签、搜索入口和 RSS/Sitemap 的代表性路由矩阵，不迁移正式内容。
- [ ] 比较 Node adapter、独立服务拓扑、缓存、断网降级、图片处理和冷启动。
- [ ] 分别比较匿名、登录、缓存命中和失效状态，始终保持静态回退。
- [ ] 代表性证据完整后，只有第二份生产扩围 ADR 可以把 C 全站 SSR 选为生产路线；否则保持 B。

## 已完成的 V1 公开站阶段

### 01　生产搜索

- [x] 在构建时生成三语文章索引，只收录允许公开列出的文章元数据。
- [x] 在全局导航加入搜索按钮和原生对话框。
- [x] 用户打开搜索后，才加载搜索脚本与对应语言的索引。
- [x] 支持键盘打开、关闭、结果导航、空结果提示和焦点返回。
- [x] 确认普通页面初始加载不包含搜索索引。

### 02　归档页迁入 A 方案

- [x] 保留真实年份和文章顺序，完成桌面与手机布局。
- [x] 处理没有文章的语言版本。

### 03　分类页迁入 A 方案

- [x] 完成分类目录页。
- [x] 完成单个分类的文章列表页。

### 04　标签页迁入 A 方案

- [x] 完成标签目录页。
- [x] 完成单个标签的文章列表页。

### 05　关于页迁入 A 方案

- [x] 整理 Morii 愿意公开的自述、站点说明和外部链接。
- [x] 不公开没有明确授权的个人信息。

### 06　正式文章阅读页

- [x] 把 A 方案文章首屏、目录、正文和侧栏迁入正式文章路由。
- [x] 显示发布日期、更新时间、分类、标签和真实翻译状态。
- [x] 加入上一篇、下一篇与返回文章列表。
- [x] 在 375、390、768、1024、1440 像素宽度验收长文。

## 保留的公开站稳定化清单

### 07　图片与灯箱

- [~] PhotoSwipe 已按文章内容标记动态加载。
- [ ] 验收多图、长图、键盘操作、缩放和焦点返回。
- [ ] 确认无图片文章不下载灯箱资源。

### 08　代码、数学与 Mermaid

- [~] Expressive Code、行号、折叠、KaTeX 和 Mermaid 已有实现。
- [ ] 分别验收复制按钮、暗色主题、无效 Mermaid 回退和移动端溢出。
- [ ] 确认普通文章不下载 Mermaid 客户端代码。

### 09　扩展内容块

- [~] 提示块、剧透、GitHub 仓库卡片、视频和音乐卡片已有实现。
- [ ] 逐项验收无 JavaScript 回退。
- [ ] 视频和音乐必须点击后才连接第三方服务。
- [ ] GitHub 数据获取失败时保留普通仓库链接。
- [ ] 复核外部服务允许列表和 CSP。

### 10　加密文章

- [~] 本地加密脚本、公开密文集合和浏览器解密流程已有实现与测试。
- [ ] 按 A 方案重做解锁界面并完成三语人工验收。
- [ ] 检查错误口令、损坏密文、键盘操作和解密后的阅读模块。
- [ ] 再次确认构建产物、日志和 Git 历史中没有明文、口令或私密路径。

### 11　可选复制限制

- [~] 正文复制限制已有实现，代码块仍允许复制。
- [ ] 验收三语提示、移动端行为和无 JavaScript 情况。
- [ ] 文档中继续明确：这只是阅读界面限制，不是安全措施。

### 12　多语言与翻译关系

- [~] `translationKey`、缺失翻译提示和文章语言链接已有实现。
- [ ] 语言切换尽量保留当前页面，不要总是返回首页。
- [ ] 检查 `hreflang`、canonical、RSS 与 Sitemap 的语言关系。
- [ ] 不生成占位翻译，不复制别的语言正文。

### 13　Pages CMS 写作流程

- [~] 公共文章的 `.pages.yml`、字段校验和媒体目录已有配置。
- [ ] 用测试分支实际走一遍创建、预览、修改、撤回草稿的流程。
- [ ] 明确图片发布前的去 EXIF/GPS 步骤。
- [ ] 加密文章继续只允许本地生成，不接入托管 CMS。

### 14　发布前总验收

- [ ] 删除或隔离 `/design/` 原型页面，确保不会进入正式索引。
- [ ] 清理未使用样式与大型分包警告。
- [ ] 完成键盘、焦点、语义标题、对比度、减弱动画和五档宽度验收。
- [ ] 运行 `pnpm check`、`pnpm test`、`pnpm build`、`pnpm links` 和隐私审计。
- [ ] 检查静态产物在 Nginx 下不依赖 Node、数据库、PM2 或搜索服务。
- [ ] Morii 明确批准后，才创建提交、推送、发布或部署。

## 明确不做

以下边界在 Morii 批准新的 ADR 并更新项目合同前继续有效：

- 评论。
- 没有明确读者价值的公开注册和用户资料系统。
- 未经 Phase 5 ADR 批准就把运行时管理后台、数据库或 API 接入生产。
- 统计分析与追踪脚本。
- 未经 Morii 单独批准的第三方服务。

## 下一块

Phase 1 已获批准并开工（2026-08-29），`prototypes/` 骨架与隔离验证已完成。

fixture corpus 已完成，`shared/` 的 frontmatter 契约也已随校验器落地（`prototypes/shared/content-schema.ts`，带与 `src/content.config.ts` 的漂移检查）。

类型检查的隔离也已完成：`prototypes/` 排除出根 `tsconfig.json`，自带一份同等严格的配置由 `pnpm -C prototypes check` 运行（ADR 13.4，L2 单独提交）。

语料的渲染基线也已完成（ADR 13.5，基准取公开文章管线，并与 `dist/` 的真实产物比对过）。

`shared/` 契约也已落地（ADR 13.6）：内容块清单、三语关系、媒体形状、错误模型。

原型 B 的薄存储层（ADR 13.7）、安全边界（13.8）与 HTTP 边界（13.9）依次接通，登录、列表、保存、自动保存、发布、回滚都经过真实 socket 集成测试。

Tiptap 接入与 round-trip 丢失计数见 ADR 13.10、13.11：未加扩展的 Beta 基线丢 3 个块，补上不透明源节点后 11 个块逐字节一致，只差末尾一个换行。按 Morii 的定夺（13.12），源码块改成可编辑文本，图片做成带预览的真节点。

**原型 B 现在可以被实际操作**（ADR 13.13）：`pnpm -C prototypes dev:b`，浏览器开 <http://localhost:4320/>，口令 `moriium-prototype`。库用夹具语料播种四篇中日文章。

**2026-08-30，Morii 实际使用后选定 B 为 vNext 最终路线，原型 A 取消。**这个决定结束 A/B 比较，不等于批准把当前尖峰直接部署到公网；生产合同仍须由 05 的 B Hybrid ADR 更新。

同日，Codex 完成了三项待补中的前两项：**完整发布闸门**（ADR 13.15）把 `shared/` 的内容、翻译关系与媒体契约接进发布事务，不合格内容既发不出去也不留审计行；**图片属性面板**（ADR 13.16）让 `alt` 终于改得了。那一轮额度耗尽，文档由 Claude 依据提交内容与重跑的检查补记。当前 `prototypes` 共 110 个用例、27 个套件，`pnpm verify` 仍是退出码 0，生产文件零改动。

复核时补测出一处闸门的过度拦截：图片引用是正则扫全文，围栏代码块里的示例图片会被当成真实引用，于是一篇讲 Markdown 语法的文章会被拒。方向是安全的一侧，但接生产前要改成按解析结果取图片引用。

**下一块固定为草稿的生产同源预览。**关键约束是不要另写一套渲染：`prototypes/tools/build-baselines.mjs` 的 `createPublicRenderer()` 已经从 `astro.config.mjs` 取生产自己的管线，`prototypes/fixtures/baseline/*.html` 已经是可断言的基准，预览要复用它们，并证明产出与基线一致。预览端点必须要会话。要预先讲明这是渲染同源，不是外观同源——站点外壳与样式表不在原型里。

之后是把 T1–T10 改成 B 的验收清单，再进入 05 的生产架构 ADR。00 节其余未完成项——固定口径的体积/构建测量、把 07–12 的人工验收测试化、安全与恢复要求——仍须在影子系统前补齐。
