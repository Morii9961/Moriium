---
import type { GetStaticPaths } from 'astro';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import AboutActivity from '../../../components/AboutActivity.astro';
import AboutChannelIcon from '../../../components/AboutChannelIcon.astro';
import AboutFriends from '../../../components/AboutFriends.astro';
import { SITE, type Language } from '../../../data/site';

export const getStaticPaths: GetStaticPaths = () =>
  SITE.languages.map((lang) => ({ params: { lang }, props: { lang } }));

const { lang } = Astro.props as { lang: Language };

const CHANNELS = [
  {
    name: 'GitHub',
    handle: 'github.com/Morii9961',
    href: 'https://github.com/Morii9961',
  },
  {
    name: 'X',
    handle: 'x.com/morii9961',
    href: 'https://x.com/morii9961',
  },
  {
    name: 'Bilibili',
    handle: 'space.bilibili.com/670549003',
    href: 'https://space.bilibili.com/670549003',
  },
] as const;

const copy = {
  zh: {
    title: '这个地方',
    intro: 'Moriium 是 Morii 的私人数字档案。文章、照片、旅行记录与技术笔记，陆续归在这里。',
    place: 'This place',
    statement: '写下的、拍下的，慢慢留下。',
    body: [
      'Moriium 使用 Astro、TypeScript 与原生 HTML/CSS 构建。公开页面和搜索索引都在构建时生成，阅读端不依赖 Node 或数据库。',
      '作者后台单独使用 Vue 3 与 Tiptap，通过 Node 与 SQLite 按需运行；后台代码不会进入公开页面。',
    ],
    reach: '其他地址', reachNote: 'Elsewhere',
    channelNotes: {
      GitHub: '源码与公开问题记录。',
      X: '偶尔更新近况。',
      Bilibili: '视频存放处。',
    },
  },
  ja: {
    title: 'この場所',
    intro: 'Moriium は Morii の個人アーカイブです。文章、写真、旅の記録、技術ノートを少しずつ収めています。',
    place: 'This place',
    statement: '書いたもの、撮ったものを、少しずつ残す。',
    body: [
      'Moriium は Astro、TypeScript、素の HTML/CSS で構築しています。公開ページと検索索引はビルド時に生成され、閲覧側は Node やデータベースに依存しません。',
      '作者用の管理画面には Vue 3 と Tiptap を使用し、Node と SQLite 上で別に動作します。管理画面のコードが公開ページに含まれることはありません。',
    ],
    reach: 'ほかの場所', reachNote: 'Elsewhere',
    channelNotes: {
      GitHub: 'ソースコードと公開の課題記録。',
      X: '近況をときどき更新します。',
      Bilibili: '動画の保管場所。',
    },
  },
  en: {
    title: 'This place',
    intro: 'Moriium is Morii’s private archive of writing, photographs, travel records, and technical notes.',
    place: 'This place',
    statement: 'What is written and photographed is kept here over time.',
    body: [
      'Moriium is built with Astro, TypeScript, and native HTML/CSS. Public pages and search indexes are generated at build time, so reading does not depend on Node or a database.',
      'The author admin uses Vue 3 and Tiptap, running separately on Node and SQLite. Its code is kept out of public pages.',
    ],
    reach: 'Elsewhere', reachNote: 'Morii online',
    channelNotes: {
      GitHub: 'Source code and public issue records.',
      X: 'Occasional updates.',
      Bilibili: 'Video archive.',
    },
  },
} as const;
const c = copy[lang];
---

<BaseLayout lang={lang} title={c.title}>
  <div class="a-directory a-about-page">
    <header class="a-directory__hero">
      <p class="prototype-kicker">Moriium / About</p>
      <div class="a-directory__title"><h1>{c.title}</h1><p>{c.intro}</p></div>
    </header>

    <section class="a-about-page__statement" aria-labelledby="about-statement">
      <p class="prototype-kicker">{c.place}</p>
      <h2 id="about-statement">{c.statement}</h2>
      <div>{c.body.map((paragraph) => <p>{paragraph}</p>)}</div>
    </section>

    <section class="a-about-page__band" aria-labelledby="about-reach">
      <header class="a-about-page__head"><h2 id="about-reach">{c.reach}</h2><p>{c.reachNote}</p></header>
      <ul class="a-about-page__channels">
        {CHANNELS.map((channel) => (
          <li>
            <a href={channel.href} rel="me">
              <AboutChannelIcon name={channel.name} />
              <span class="a-about-page__channel-copy">
                <strong>{channel.name}</strong>
                <span class="a-about-page__channel-note">{c.channelNotes[channel.name]}</span>
                <span class="a-about-page__channel-handle">{channel.handle}</span>
              </span>
              <span class="a-about-page__channel-arrow" aria-hidden="true">↗</span>
            </a>
          </li>
        ))}
      </ul>
    </section>

    <AboutActivity lang={lang} />

    <AboutFriends lang={lang} />

  </div>
</BaseLayout>
